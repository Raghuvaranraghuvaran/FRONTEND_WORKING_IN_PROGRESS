# Set this when the backend is ready. Merchant setup will POST to {VITE_API_URL}/merchants
# and persist the returned merchant id in the database.
# Leave empty to use the local mock API.
VITE_API_URL=
